<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/content_favorite"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.ucu.twentyfourseven.favorites.FavoriteActivity"
    tools:showIn="@layout/activity_favorite"
    android:orientation="vertical">

    <TextView android:layout_height="wrap_content" android:id="@+id/TextViewSubtotal"
        android:layout_width="fill_parent"
        android:textColor="@android:color/holo_orange_dark" android:text="Total of Favorites:" android:textStyle="bold"
        android:textSize="24sp"
        android:layout_marginTop="50dp"
        android:layout_alignParentTop="true"
        android:layout_alignLeft="@+id/LinearLayoutCheckout"
        android:layout_alignStart="@+id/LinearLayoutCheckout"></TextView>

    <ListView
        android:layout_height="358dp"
        android:id="@+id/ListViewFavorites"
        android:layout_width="fill_parent"
        android:background="#ffffff"
        android:cacheColorHint="#ffffff"
        android:clickable="true"
        android:choiceMode="multipleChoice"
        android:layout_above="@+id/LinearLayoutCheckout"
        android:layout_marginTop="15dp">

    </ListView>

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:layout_margin="5dip"
        android:id="@+id/LinearLayoutCheckout"
        android:layout_gravity="right"
        android:layout_alignParentBottom="true">

        <Button android:layout_width="wrap_content"
            android:layout_height="wrap_content" android:id="@+id/ButtonRemoveFromCart"
            android:text="Remove"
            android:background="@color/colorAccent"
            android:textStyle="normal|bold"
            android:textColor="@color/cardview_light_background"
            android:paddingLeft="50dp"
            android:paddingRight="50dp"
            android:layout_alignParentBottom="true"
            android:layout_centerHorizontal="true"></Button>

    </RelativeLayout>

</LinearLayout>
